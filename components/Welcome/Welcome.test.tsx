
describe('Welcome component', () => {
  it('has correct Next.js theming section link', () => {
    // render(<Welcome text='test'/>);
    // expect(screen.getByText('this guide')).toHaveAttribute(
    //   'href',
    //   'https://mantine.dev/guides/next/'
    // );
  });
});
